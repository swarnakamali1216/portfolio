# ── Server ──────────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── Gmail SMTP (App Password — NOT your real password) ──
# Step 1: Turn on 2FA → myaccount.google.com/security
# Step 2: Generate App Password → myaccount.google.com/apppasswords
GMAIL_USER=swarnakamali12@gmail.com
GMAIL_PASS=sgny mxoi gora udtn

# Email that RECEIVES contact messages (usually same as GMAIL_USER)
RECEIVER_EMAIL=swarnakamali12@gmail.com

# ── MongoDB (optional — for storing contact messages) ──
MONGO_URI=mongodb://localhost:27017/swarna-portfolio

# ── Frontend URL (for CORS) ──────────────────────────
FRONTEND_URL=http://localhost:3000
